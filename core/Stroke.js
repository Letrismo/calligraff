class Stroke{constructor(settings={}){this.id=crypto.randomUUID?crypto.randomUUID():String(Date.now());this.settings=structuredClone(settings);this.samples=[];this.arcLength=0}add(sample){const last=this.samples[this.samples.length-1];if(last){const dx=sample.x-last.x;const dy=sample.y-last.y;this.arcLength+=Math.hypot(dx,dy)}this.samples.push(sample)}get length(){return this.samples.length}getLast(count){return this.samples.slice(Math.max(0,this.samples.length-count))}}