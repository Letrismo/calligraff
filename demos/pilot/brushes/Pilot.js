class Pilot extends EQuill{constructor(settings={}){super({...PILOT_CONFIG.brush,...settings});this.renderer=new CircleStampRenderer();this.lastRendered=null;this.distanceCarry=0;this.gradientDistance=0}begin(sample){super.begin(sample);this.lastRendered={x:sample.x,y:sample.y};this.distanceCarry=0;this.gradientDistance=0}renderIncrement(graphics){const pts=this.stroke.getLast(4);if(pts.length<4)return;const[p0,p1,p2,p3]=pts;const step=this.settings.smoothing;let prev=this.lastRendered||{x:p1.x,y:p1.y};for(let t=0;t<=1;t+=step){const point=this.catmullRom(p0,p1,p2,p3,t);this.renderSegment(graphics,prev,point);prev=point}this.lastRendered=prev}renderSegment(graphics,from,to){const dx=to.x-from.x;const dy=to.y-from.y;const distance=Math.hypot(dx,dy);if(distance===0)return;const spacing=Math.max(1,this.settings.size*this.settings.spacing);const steps=Math.floor((distance+this.distanceCarry)/spacing);if(steps<=0){this.distanceCarry+=distance;return}for(let i=1;i<=steps;i++){const d=(i*spacing-this.distanceCarry)/distance;if(d<0||d>1)continue;const x=lerp(from.x,to.x,d);const y=lerp(from.y,to.y,d);this.gradientDistance+=spacing;this.renderer.render(graphics,{x,y,size:this.settings.size,color:this.resolveColor(),alpha:this.settings.alpha,compositeOperation:this.settings.compositeOperation})}this.distanceCarry=(distance+this.distanceCarry)%spacing}resolveColor(){if(this.settings.colorMode==="solid")return this.settings.color;const palette=this.settings.palette;if(!palette||palette.length===0)return this.settings.color;if(palette.length===1)return palette[0];const cycle=900;const position=(this.gradientDistance%cycle)/cycle;const scaled=position*palette.length;const index=Math.floor(scaled)%palette.length;const next=(index+1)%palette.length;const amt=scaled-Math.floor(scaled);return lerpColor(color(palette[index]),color(palette[next]),amt)}catmullRom(p0,p1,p2,p3,t){const t2=t*t,t3=t2*t;return{x:.5*(2*p1.x+(-p0.x+p2.x)*t+(2*p0.x-5*p1.x+4*p2.x-p3.x)*t2+(-p0.x+3*p1.x-3*p2.x+p3.x)*t3),y:.5*(2*p1.y+(-p0.y+p2.y)*t+(2*p0.y-5*p1.y+4*p2.y-p3.y)*t2+(-p0.y+3*p1.y-3*p2.y+p3.y)*t3)}}}